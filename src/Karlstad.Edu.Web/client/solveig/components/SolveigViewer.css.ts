import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the SolveigViewer component
 */
export const SolveigViewerStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(SolveigViewerStyles, {
    container: {
        fontSize: 22
    }
})

