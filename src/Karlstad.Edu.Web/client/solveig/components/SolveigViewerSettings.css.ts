import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the SolveigViewerSettings component
 */
export const SolveigViewerSettingsStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(SolveigViewerSettingsStyles, {
    container: {
        fontSize: 22
    }
})

