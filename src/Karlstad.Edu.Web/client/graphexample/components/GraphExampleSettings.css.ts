import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the GraphExampleSettings component
 */
export const GraphExampleSettingsStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(GraphExampleSettingsStyles, {
    container: {
        fontSize: 22
    }
})

