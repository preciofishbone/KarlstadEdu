import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the GraphExample component
 */
export const GraphExampleStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(GraphExampleStyles, {
    container: {
        fontSize: 22
    }
})

